import { Environment } from '../app/models/environment';

export const environment: Environment = {
  production: true,
  consoleSettings: {
    log: false,
    info: false,
    warn: false,
    debug: false,
  },
};
